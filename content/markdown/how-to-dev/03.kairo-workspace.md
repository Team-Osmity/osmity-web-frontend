# Kairo-workspace

Kairo.ts では大量のアドオンを扱います。
つまり、大量のgithub repositoryを使う可能性が考えられます。

でも、それだと何回もgit clone したり、npm run build したりと大変すぎるため、
大量のリポジトリ(フォルダ) で同じコマンドを一度で実行できるようにしたのが、
**Kairo-workspace** です。

この Kairo-workspace を使って、簡単にKairo.tsの開発環境を整備できます。

